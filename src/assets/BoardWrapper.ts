import styled from 'styled-components/macro';

const BoardWrapper = styled.div`
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  width: 300px;
  margin: auto;
`;

export default BoardWrapper;
